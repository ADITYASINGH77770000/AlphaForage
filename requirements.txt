# AlphaForge — requirements entry point
#
# For most users (UI + backtest + factors + portfolio):
#   pip install -r requirements-core.txt
#
# For prediction (LSTM) pages (optional heavy deps):
#   pip install -r requirements-ml.txt     (includes core automatically)
#
# Default deployment target (including Streamlit Community Cloud):
# keep installs lean and rely on the core analytics stack only.
-r requirements-core.txt
